{
	"name": "main",
	"version": "1.0.0",
	"description": "",
	"keywords": [],
	"license": "MIT",
	"author": "",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"prepare": "git config --local --get core.hooksPath >/dev/null 2>&1 || vp config",
		"dev": "pnpm run -r dev",
		"build": "pnpm run -r build",
		"preview": "pnpm run -r preview",
		"tsc": "pnpm run -r tsc",
		"knip": "knip --production --cache",
		"test": "pnpm vp test"
	},
	"devDependencies": {
		"@vitest/coverage-v8": "4.1.8",
		"knip": "catalog:",
		"vite": "catalog:",
		"vite-plus": "catalog:"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.10.0",
			"onFail": "download"
		}
	}
}
