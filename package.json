{
	"name": "main",
	"version": "1.0.0",
	"description": "",
	"keywords": [],
	"license": "MIT",
	"author": "",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"prepare": "git config --local --get core.hooksPath >/dev/null 2>&1 || vp config",
		"dev": "pnpm run -r dev",
		"storybook": "pnpm run -r storybook",
		"build": "pnpm run -r build",
		"preview": "pnpm run -r preview",
		"tsc": "pnpm run -r tsc",
		"knip": "knip --production --cache",
		"check": "vp check",
		"test": "vp run vp:test",
		"test:changed": "vp run vp:test:changed"
	},
	"devDependencies": {
		"@commitlint/cli": "catalog:",
		"@commitlint/config-conventional": "catalog:",
		"@commitlint/types": "catalog:",
		"@playwright/test": "catalog:",
		"@storybook/addon-vitest": "catalog:",
		"@vitest/browser-playwright": "catalog:",
		"@vitest/coverage-v8": "catalog:",
		"knip": "catalog:",
		"vite": "catalog:",
		"vite-plus": "catalog:"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.10.0",
			"onFail": "download"
		}
	}
}
